[project]
name = "rag"
version = "0.1.0"
description = "RAG Ollama usando RustKissVDB como memoria persistente."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pymupdf>=1.26.7",
    "python-dotenv>=1.2.1",
]
